{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "python -c \"from app import create_app, db; app = create_app(); app.app_context().push(); db.create_all()\" && flask db stamp head && gunicorn run:app --bind 0.0.0.0:$PORT --workers 2",
    "region": "europe-west4",
    "restartPolicyType": "ON_FAILURE"
  }
}
